# Day 2 双端布局实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**目标：** 搭建主应用的双端响应式布局框架（PC 侧边栏 + 移动端底部导航），建立路由骨架页面，初始化 Zustand 全局状态基础结构。

**架构：** Next.js 15 App Router + Tailwind 响应式布局 + Zustand 状态管理。PC 端使用固定侧边栏（240px），移动端使用底部导航（56px），通过 Tailwind 的 `md:` 断点切换。

**技术栈：** Next.js 15, TypeScript, Tailwind CSS, Shadcn/UI, Zustand, Lucide React

---

## 当前状态速览

| 文件/目录 | 状态 | 说明 |
|----------|------|------|
| `src/app/(main)/` | 存在 | 需创建 layout.tsx 和骨架页面 |
| `src/app/admin/` | 存在 | 需创建 layout.tsx 和骨架页面 |
| `src/components/ui/` | 已安装 | Shadcn 组件已就绪 |
| `src/store/` | 不存在 | 需创建 Zustand store |
| `src/components/layout/` | 不存在 | 需创建布局组件 |
| `zustand` | 已安装 | ✅ |
| `lucide-react` | 已安装 | ✅ |

---

## Task 1: 创建 Zustand Store（Auth 状态管理）

**Files:**
- Create: `src/store/index.ts`
- Create: `src/store/auth.ts`

**Step 1: 创建 store 目录**

```bash
mkdir -p src/store
```

Expected: 目录创建成功

**Step 2: 写入 auth store**

```typescript
// src/store/auth.ts
import { create } from 'zustand'
import type { Profile } from '@/types'

interface AuthStore {
  profile: Profile | null
  setProfile: (profile: Profile | null) => void
  updateCredits: (newBalance: number) => void
}

export const useAuthStore = create<AuthStore>((set) => ({
  profile: null,
  setProfile: (profile) => set({ profile }),
  updateCredits: (newBalance) =>
    set((state) => ({
      profile: state.profile ? { ...state.profile, credits: newBalance } : null,
    })),
}))
```

**Step 3: 写入统一导出**

```typescript
// src/store/index.ts
export { useAuthStore } from './auth'
```

**Step 4: 验证文件创建**

```bash
ls src/store/
```

Expected: `auth.ts` 和 `index.ts` 文件存在

---

## Task 2: 创建布局组件 - TopBar

**Files:**
- Create: `src/components/layout/top-bar.tsx`

**Step 1: 创建 layout 目录**

```bash
mkdir -p src/components/layout
```

**Step 2: 写入 TopBar 组件**

```tsx
// src/components/layout/top-bar.tsx
'use client'

import Link from 'next/link'
import { useRouter } from 'next/navigation'
import { LogOut } from 'lucide-react'
import { toast } from 'sonner'
import { createClient } from '@/lib/supabase/client'
import { Button } from '@/components/ui/button'
import { Badge } from '@/components/ui/badge'
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuLabel,
  DropdownMenuSeparator,
  DropdownMenuTrigger,
} from '@/components/ui/dropdown-menu'
import { Avatar, AvatarFallback } from '@/components/ui/avatar'
import type { Profile } from '@/types'

interface TopBarProps {
  profile: Profile
}

export function TopBar({ profile }: TopBarProps) {
  const router = useRouter()

  async function handleLogout() {
    const supabase = createClient()
    await supabase.auth.signOut()
    toast.success('已退出登录')
    router.push('/login')
    router.refresh()
  }

  return (
    <header className="fixed top-0 left-0 right-0 z-40 h-14 md:h-14 border-b bg-white">
      <div className="flex items-center justify-between h-full px-4 md:px-6">
        {/* Logo */}
        <Link href="/" className="flex items-center gap-2 font-bold text-lg">
          <span className="text-2xl">🪨</span>
          <span className="hidden sm:inline">Crusher</span>
        </Link>

        {/* Right: Credits + User Menu */}
        <div className="flex items-center gap-3">
          {/* Credits Badge */}
          <Badge variant="secondary" className="text-sm font-medium">
            {profile.credits} 积分
          </Badge>

          {/* User Menu */}
          <DropdownMenu>
            <DropdownMenuTrigger asChild>
              <Button variant="ghost" size="icon" className="rounded-full">
                <Avatar className="h-8 w-8">
                  <AvatarFallback>
                    {profile.username?.[0]?.toUpperCase() || 'U'}
                  </AvatarFallback>
                </Avatar>
              </Button>
            </DropdownMenuTrigger>
            <DropdownMenuContent align="end" className="w-48">
              <DropdownMenuLabel>
                <div className="flex flex-col">
                  <span className="text-sm font-medium">
                    {profile.username || '用户'}
                  </span>
                  <span className="text-xs text-zinc-500">{profile.role}</span>
                </div>
              </DropdownMenuLabel>
              <DropdownMenuSeparator />
              <DropdownMenuItem onClick={handleLogout}>
                <LogOut className="mr-2 h-4 w-4" />
                退出登录
              </DropdownMenuItem>
            </DropdownMenuContent>
          </DropdownMenu>
        </div>
      </div>
    </header>
  )
}
```

---

## Task 3: 创建布局组件 - Sidebar（PC 端）

**Files:**
- Create: `src/components/layout/sidebar.tsx`

**Step 1: 写入 Sidebar 组件**

```tsx
// src/components/layout/sidebar.tsx
'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { Home, Plus, FileText, User, Sparkles, CreditCard } from 'lucide-react'
import { cn } from '@/lib/utils'
import { Button } from '@/components/ui/button'
import { Separator } from '@/components/ui/separator'

const navItems = [
  { href: '/', label: '首页', icon: Home },
  { href: '/capture', label: '新建', icon: Plus },
  { href: '/documents', label: '文档', icon: FileText },
  { href: '/profile', label: '我的', icon: User },
  { href: '/lenses', label: '透镜管理', icon: Sparkles },
]

export function Sidebar() {
  const pathname = usePathname()

  return (
    <aside className="hidden md:flex md:w-60 md:flex-col fixed inset-y-0 z-50 border-r bg-white pt-14">
      <nav className="flex-1 flex flex-col gap-1 p-4">
        {navItems.map((item) => {
          const Icon = item.icon
          const isActive = pathname === item.href

          return (
            <Link key={item.href} href={item.href}>
              <Button
                variant={isActive ? 'secondary' : 'ghost'}
                className={cn(
                  'w-full justify-start gap-3',
                  isActive && 'bg-zinc-100'
                )}
              >
                <Icon className="h-5 w-5" />
                {item.label}
              </Button>
            </Link>
          )
        })}
      </nav>

      <Separator />

      {/* Bottom: Recharge Link */}
      <div className="p-4">
        <Link href="/profile">
          <Button variant="outline" className="w-full gap-2">
            <CreditCard className="h-4 w-4" />
            充值积分
          </Button>
        </Link>
      </div>
    </aside>
  )
}
```

---

## Task 4: 创建布局组件 - BottomNav（移动端）

**Files:**
- Create: `src/components/layout/bottom-nav.tsx`

**Step 1: 写入 BottomNav 组件**

```tsx
// src/components/layout/bottom-nav.tsx
'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { Home, Plus, FileText, User } from 'lucide-react'
import { cn } from '@/lib/utils'

const navItems = [
  { href: '/', label: '首页', icon: Home },
  { href: '/capture', label: '新建', icon: Plus },
  { href: '/documents', label: '文档', icon: FileText },
  { href: '/profile', label: '我的', icon: User },
]

export function BottomNav() {
  const pathname = usePathname()

  return (
    <nav className="flex md:hidden fixed bottom-0 left-0 right-0 z-50 h-14 border-t bg-white">
      {navItems.map((item) => {
        const Icon = item.icon
        const isActive = pathname === item.href

        return (
          <Link
            key={item.href}
            href={item.href}
            className={cn(
              'flex-1 flex flex-col items-center justify-center gap-1 text-xs',
              isActive ? 'text-zinc-900' : 'text-zinc-500'
            )}
          >
            <Icon className="h-5 w-5" />
            <span>{item.label}</span>
          </Link>
        )
      })}
    </nav>
  )
}
```

---

## Task 5: 创建主布局 - (main)/layout.tsx

**Files:**
- Create: `src/app/(main)/layout.tsx`

**Step 1: 写入主布局**

```tsx
// src/app/(main)/layout.tsx
import { redirect } from 'next/navigation'
import { getCurrentProfile } from '@/lib/supabase/server'
import { TopBar } from '@/components/layout/top-bar'
import { Sidebar } from '@/components/layout/sidebar'
import { BottomNav } from '@/components/layout/bottom-nav'

export default async function MainLayout({
  children,
}: {
  children: React.ReactNode
}) {
  const profile = await getCurrentProfile()

  if (!profile) {
    redirect('/login')
  }

  return (
    <div className="min-h-screen bg-zinc-50">
      {/* TopBar */}
      <TopBar profile={profile} />

      {/* Sidebar (PC only) */}
      <Sidebar />

      {/* Main Content */}
      <main className="flex-1 md:ml-60 pt-14 pb-16 md:pb-0">
        {children}
      </main>

      {/* BottomNav (Mobile only) */}
      <BottomNav />
    </div>
  )
}
```

---

## Task 6: 创建骨架页面 - 首页

**Files:**
- Create: `src/app/(main)/page.tsx`

**Step 1: 写入首页骨架**

```tsx
// src/app/(main)/page.tsx
import Link from 'next/link'
import { Button } from '@/components/ui/button'
import { Card, CardContent } from '@/components/ui/card'
import { Plus } from 'lucide-react'

export default function HomePage() {
  return (
    <div className="container max-w-4xl mx-auto p-4 md:p-8">
      <Card>
        <CardContent className="flex flex-col items-center justify-center py-16 text-center">
          <div className="text-6xl mb-4">📄</div>
          <h2 className="text-xl font-semibold mb-2">还没有文档</h2>
          <p className="text-zinc-500 mb-6">点击新建开始创建你的第一份文档</p>
          <Link href="/capture">
            <Button size="lg" className="gap-2">
              <Plus className="h-5 w-5" />
              新建文档
            </Button>
          </Link>
        </CardContent>
      </Card>
    </div>
  )
}
```

---

## Task 7: 创建骨架页面 - 输入页

**Files:**
- Create: `src/app/(main)/capture/page.tsx`

**Step 1: 写入输入页骨架**

```tsx
// src/app/(main)/capture/page.tsx
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

export default function CapturePage() {
  return (
    <div className="container max-w-4xl mx-auto p-4 md:p-8">
      <Card>
        <CardHeader>
          <CardTitle>新建文档</CardTitle>
        </CardHeader>
        <CardContent>
          <div className="flex items-center justify-center py-16 text-zinc-500">
            输入区域（Day 3 实现）
          </div>
        </CardContent>
      </Card>
    </div>
  )
}
```

---

## Task 8: 创建骨架页面 - 文档详情

**Files:**
- Create: `src/app/(main)/documents/[id]/page.tsx`

**Step 1: 创建 documents 目录**

```bash
mkdir -p src/app/\(main\)/documents/\[id\]
```

**Step 2: 写入文档详情骨架**

```tsx
// src/app/(main)/documents/[id]/page.tsx
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

export default function DocumentDetailPage({
  params,
}: {
  params: { id: string }
}) {
  return (
    <div className="container max-w-4xl mx-auto p-4 md:p-8">
      <Card>
        <CardHeader>
          <CardTitle>文档详情</CardTitle>
        </CardHeader>
        <CardContent>
          <div className="flex items-center justify-center py-16 text-zinc-500">
            文档详情（Day 3 实现）
            <br />
            Document ID: {params.id}
          </div>
        </CardContent>
      </Card>
    </div>
  )
}
```

---

## Task 9: 创建骨架页面 - 透镜管理

**Files:**
- Create: `src/app/(main)/lenses/page.tsx`

**Step 1: 创建 lenses 目录**

```bash
mkdir -p src/app/\(main\)/lenses
```

**Step 2: 写入透镜管理骨架**

```tsx
// src/app/(main)/lenses/page.tsx
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

export default function LensesPage() {
  return (
    <div className="container max-w-4xl mx-auto p-4 md:p-8">
      <Card>
        <CardHeader>
          <CardTitle>透镜管理</CardTitle>
        </CardHeader>
        <CardContent>
          <div className="flex items-center justify-center py-16 text-zinc-500">
            透镜管理（Day 6 实现）
          </div>
        </CardContent>
      </Card>
    </div>
  )
}
```

---

## Task 10: 创建骨架页面 - 个人资料

**Files:**
- Create: `src/app/(main)/profile/page.tsx`

**Step 1: 创建 profile 目录**

```bash
mkdir -p src/app/\(main\)/profile
```

**Step 2: 写入个人资料骨架**

```tsx
// src/app/(main)/profile/page.tsx
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

export default function ProfilePage() {
  return (
    <div className="container max-w-4xl mx-auto p-4 md:p-8">
      <Card>
        <CardHeader>
          <CardTitle>个人资料</CardTitle>
        </CardHeader>
        <CardContent>
          <div className="flex items-center justify-center py-16 text-zinc-500">
            个人资料（Day 5 实现）
          </div>
        </CardContent>
      </Card>
    </div>
  )
}
```

---

## Task 11: 创建管理后台布局

**Files:**
- Create: `src/app/admin/layout.tsx`

**Step 1: 写入管理后台布局**

```tsx
// src/app/admin/layout.tsx
import { redirect } from 'next/navigation'
import Link from 'next/link'
import { getCurrentProfile } from '@/lib/supabase/server'
import { Button } from '@/components/ui/button'
import { Home } from 'lucide-react'

export default async function AdminLayout({
  children,
}: {
  children: React.ReactNode
}) {
  const profile = await getCurrentProfile()

  if (!profile || profile.role !== 'admin') {
    redirect('/')
  }

  return (
    <div className="min-h-screen bg-zinc-50">
      {/* Simple Admin TopBar */}
      <header className="fixed top-0 left-0 right-0 z-40 h-14 border-b bg-white">
        <div className="flex items-center justify-between h-full px-4 md:px-6">
          <div className="flex items-center gap-4">
            <span className="text-2xl">🪨</span>
            <span className="font-bold text-lg">Crusher 管理后台</span>
          </div>
          <Link href="/">
            <Button variant="ghost" size="sm" className="gap-2">
              <Home className="h-4 w-4" />
              返回主应用
            </Button>
          </Link>
        </div>
      </header>

      {/* Content */}
      <main className="pt-14">
        {children}
      </main>
    </div>
  )
}
```

---

## Task 12: 创建管理后台首页骨架

**Files:**
- Create: `src/app/admin/page.tsx`

**Step 1: 写入管理后台首页**

```tsx
// src/app/admin/page.tsx
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

export default function AdminPage() {
  return (
    <div className="container max-w-6xl mx-auto p-4 md:p-8">
      <h1 className="text-2xl font-bold mb-6">管理后台</h1>

      <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">
        <Card>
          <CardHeader>
            <CardTitle>用户管理</CardTitle>
          </CardHeader>
          <CardContent>
            <p className="text-sm text-zinc-500">Day 7-8 实现</p>
          </CardContent>
        </Card>

        <Card>
          <CardHeader>
            <CardTitle>支付订单</CardTitle>
          </CardHeader>
          <CardContent>
            <p className="text-sm text-zinc-500">Day 7-8 实现</p>
          </CardContent>
        </Card>

        <Card>
          <CardHeader>
            <CardTitle>用量统计</CardTitle>
          </CardHeader>
          <CardContent>
            <p className="text-sm text-zinc-500">Day 7-8 实现</p>
          </CardContent>
        </Card>

        <Card>
          <CardHeader>
            <CardTitle>Prompt 管理</CardTitle>
          </CardHeader>
          <CardContent>
            <p className="text-sm text-zinc-500">Day 8 实现</p>
          </CardContent>
        </Card>

        <Card>
          <CardHeader>
            <CardTitle>公告管理</CardTitle>
          </CardHeader>
          <CardContent>
            <p className="text-sm text-zinc-500">Day 8 实现</p>
          </CardContent>
        </Card>

        <Card>
          <CardHeader>
            <CardTitle>反馈管理</CardTitle>
          </CardHeader>
          <CardContent>
            <p className="text-sm text-zinc-500">Day 8 实现</p>
          </CardContent>
        </Card>
      </div>
    </div>
  )
}
```

---

## Task 13: 验收检查

**Step 1: 启动开发服务器**

```bash
npm run dev
```

Expected: 服务器启动，无编译错误

**Step 2: 验证 PC 端布局（浏览器宽度 ≥768px）**

- 访问 http://localhost:3000
- 检查：左侧固定侧边栏（240px）显示
- 检查：顶部栏显示积分和用户菜单
- 检查：底部导航隐藏
- 检查：内容区右移 240px

**Step 3: 验证移动端布局（浏览器宽度 <768px）**

- 缩小浏览器窗口或使用开发者工具切换到移动设备
- 检查：侧边栏隐藏
- 检查：底部导航显示（4 个 tab）
- 检查：内容区全宽

**Step 4: 验证路由切换**

- 点击侧边栏/底部导航各个链接
- 检查：路由正确切换
- 检查：当前页面高亮显示

**Step 5: 验证骨架页面**

- 访问 `/` → 显示空状态占位
- 访问 `/capture` → 显示"输入区域（Day 3 实现）"
- 访问 `/lenses` → 显示"透镜管理（Day 6 实现）"
- 访问 `/profile` → 显示"个人资料（Day 5 实现）"

**Step 6: 验证管理后台**

- 访问 `/admin` → 显示管理后台首页（需要 admin 角色）
- 检查：简洁顶部栏显示
- 检查：6 个管理模块卡片显示

**Step 7: 验证登出功能**

- 点击顶部栏用户头像 → 下拉菜单
- 点击"退出登录"
- 检查：跳转到 /login
- 检查：再次访问 / 自动跳转到 /login

---

## Task 14: 提交代码

**Step 1: 提交所有更改**

```bash
git add -A
git commit -m "feat: Day 2 双端布局 - PC侧边栏+移动端底部导航+Zustand状态+骨架页面"
```

Expected: 提交成功

---

## 执行完成后的状态

✅ PC 端（≥768px）：侧边栏固定左侧 240px，内容区右移，顶部栏显示积分和用户菜单
✅ 移动端（<768px）：侧边栏隐藏，底部导航显示，点击 tab 路由正确切换
✅ 所有骨架页面可正常访问，无报错
✅ 登出功能正常（UserMenu → 登出 → 跳转 /login）
✅ 未登录访问 (main) 路由自动跳转 /login（middleware 保证）
✅ Zustand store 初始化完成，为 Day 3-5 的状态管理做好准备

---

## 执行选项

Plan complete and saved to `docs/plans/2026-03-05-day2-layout.md`. Two execution options:

**1. Subagent-Driven (this session)** - I dispatch fresh subagent per task, review between tasks, fast iteration

**2. Parallel Session (separate)** - Open new session with executing-plans, batch execution with checkpoints

**Which approach?**
